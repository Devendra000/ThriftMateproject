<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Review</title>
    <link rel="stylesheet" href="../sellerByMini.css">
    <style>
        /* Style the table */
table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 20px;
    background-color: #fff;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

/* Style table header (th) */
th {
    background-color: #007BFF;
    color: #fff;
    font-weight: bold;
    padding: 10px;
    text-align: left;
}

/* Style table rows (alternating background colors) */
tr:nth-child(even) {
    background-color: #f2f2f2;
}

/* Style table data cells (td) */
td {
    padding: 8px;
    border: 1px solid #ccc;
    text-align: left;
}

/* Add hover effect to rows */
tr:hover {
    background-color: #e0e0e0;
}

    </style>
</head>
<body>
    <nav>
        <label class="logo">
            <a href="/seller/home">ThriftMate</a>
        </label>
        <ul class="lower">
            <li><a href="/seller/home" class="active">Home</a></li>
            <li><a href="/seller/listProducts">My products</a></li>
            <li><a href="/seller/logout">Logout</a></li>
        </ul>

    </nav>

    <table width='50%'>
        <tr>
            <th>Product Name</th>
            <th>Price</th>
            <th>Sold quantity</th>
            <th>Stars</th>
        </tr>

        <% if(product.length > 0){
            for(i=0; i < product.length; i++){%>
                <tr>
                    <td> <%= product[i].name %></td>
                    <td> <%= product[i].price %></td>
                    <td> <%= product[i].sold %></td>
                    <td> <%= product[i].stars %></td>
                </tr>
        

                <%}}else {%>
            <tr>
                <td colspan="5">No item to show</td>
            </tr>
        <% } %>
    </table>
</body>
</html>