<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link href="../admin.css" rel="stylesheet">

    <style>
        body {
            font-family: Arial, sans-serif;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th, td {
            border: 1px solid #ccc;
            padding: 10px;
            text-align: center;
        }

        th{
            background-color: #007BFF;
        }

        .actions {
            display: flex;
            justify-content: space-between;
        }

        .accept, .reject {
            text-decoration: none;
            padding: 5px 10px;
            background-color: #4CAF50;
            color: #fff;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .reject {
            background-color: #d8584f;
        }

        .accept:hover{
            background-color: #0ce63b;
        } 
        .reject:hover {
            background-color: #f50404;
        }
    </style>
</head>
<body>
    <nav>
        <ul>
            <li><a href="/admin/buyersPage">Buyers</a></li>
            <li><a href="/admin/sellersPage">Sellers</a></li>
            <li><a href="/admin/verifyProducts">VERIFY PRODUCTS</a></li>
            <li><a href="/admin/logout">Logout</a></li>
        </ul>
    </nav>
    <table>
        <thead>
            <tr>
                <th>Product Name</th>
                <th>Publisher Email</th>
                <th>Product Price</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% if ( product.length > 0){
                for(i=0; i < product.length; i++){ %>
                    <tr>
                        <td><%=product[i].name%></td>
                        <td><%=product[i].uploader%></td>
                        <td><%=product[i].price%></td>
                        <td class="actions">
                            <a class='accept' href="/admin/accept?id=<%= product[i].id%>">Accept</a>
                            <a class='reject' href="/admin/reject?id=<%= product[i].id%>">Reject</a>
                        </td>
                    </tr>
                    <% }} else{ %>
                        <tr>
                            <td colspan="4">Nothing to approve</td>
                        </tr>

                        <% } %>

            <!-- Add more rows as needed -->
        </tbody>
    </table>
</body>
</html>
